# Version control
.git
.gitignore

# Dependencies (rebuilt inside the image)
vendor/
node_modules/

# Local secrets / environment (generated at container start)
.env

# Compose & docs that don't belong in the image
docker-compose*.yml
Prio.docx
diagramms/

# Build artifacts
*.css.map
